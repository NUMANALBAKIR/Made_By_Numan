import { LoginAlertsDirective } from './login-alerts.directive';

describe('LoginAlertsDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginAlertsDirective();
    expect(directive).toBeTruthy();
  });
});
